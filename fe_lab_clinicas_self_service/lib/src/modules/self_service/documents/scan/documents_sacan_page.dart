import 'package:flutter/material.dart';

class DocumentsSacanPage extends StatelessWidget {

  const DocumentsSacanPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Document Scan'),),
           body: Container(),
       );
  }
}